{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd WebApplication1 && dotnet restore && dotnet build -c Release"
  },
  "deploy": {
    "startCommand": "cd WebApplication1 && dotnet ef database update && dotnet run -c Release --no-build",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

